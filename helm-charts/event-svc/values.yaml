replicaCount: 2

image:
  repository: 302174038725.dkr.ecr.ap-northeast-2.amazonaws.com/ticketing/event-svc
  tag: latest
  pullPolicy: Always

service:
  port: 5000

env:
  DB_WRITER_HOST: "your-rds-endpoint"
  DB_READER_HOST: "your-rds-reader"
  DB_USER: "root"

  REDIS_HOST: "your-redis-endpoint"
  SQS_QUEUE_URL: "your-sqs-url"

  COGNITO_JSON: ""

secret:
  DB_PASSWORD: "your-password"

# 나중에 수정하기 (f->t) metrics-server 없으면 HPA 안 돌아감
autoscaling:
  enabled: false
  minReplicas: 2
  maxReplicas: 5
  targetCPUUtilizationPercentage: 70